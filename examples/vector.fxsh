# Vector-like comptime helpers in supported subset

let comptime vector_capacity = fn n -> n * 2
let comptime DEFAULT_CAP = vector_capacity 8

let grow = fn len ->
  if len < DEFAULT_CAP then DEFAULT_CAP else len * 2

42
