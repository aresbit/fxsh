# Match feature demo

type option = None | Some of int
type ilist = Nil | Cons of int * ilist

let get_or = fn opt d ->
  match opt with
  | Some x -> x
  | None -> d
  end

let head_or = fn xs d ->
  match xs with
  | Cons(x, _) -> x
  | Nil -> d
  end
