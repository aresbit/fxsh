type 'a maybe = Some of 'a | None

let id = fn x -> x

let render = fn v ->
  match v with
  | Some (n, s) -> s ++ ":" ++ (if n == 1 then "1" else "0")
  | None -> "none"
  end

print (render (Some (1, "ok")))
print (render (id (Some (1, "ok"))))
